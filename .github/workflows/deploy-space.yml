name: Deploy Hugging Face Space

# Disabled on push: HF Docker Spaces require PRO / HF_TOKEN.
# Keep manual trigger only if you later add a Write token + PRO plan.
on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Skipped by default
        run: |
          echo "HF Space auto-deploy is disabled."
          echo "Primary live host: https://clearvoice-ai-4hnp.onrender.com/"
          echo "To enable later: add HF_TOKEN secret and switch workflow back to push trigger."
